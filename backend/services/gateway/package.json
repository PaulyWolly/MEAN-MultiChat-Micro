{
  "name": "mean-multichat-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Phase 1 API gateway — proxies /api to the monolith (and later to peels)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.1",
    "http-proxy-middleware": "^3.0.5",
    "mean-multichat-micro": "file:../../.."
  }
}
