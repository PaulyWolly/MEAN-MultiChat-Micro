# Copy to .env for local overrides. Secrets stay in backend/server/.env
# (server.js loads that file first, then this one).
PORT=4806
CORS_ORIGIN=http://localhost:4200
# SPEECH_API_KEY=
# SPEECH_REGION=
# AI_TTS_MAX_CHARS=5000
