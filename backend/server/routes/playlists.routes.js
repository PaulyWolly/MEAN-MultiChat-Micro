/**
 * Playlist routes — MOVED to backend/services/media-service/routes/playlists.routes.js.
 * The monolith no longer mounts /api/playlists; the gateway proxies that path to :4805.
 */
throw new Error(
  'playlists.routes.js has moved to backend/services/media-service — do not mount from the monolith'
);
