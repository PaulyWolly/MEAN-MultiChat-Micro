/**
 * RAG routes — MOVED to backend/services/rag-service/routes/rag.routes.js (Phase 2).
 * The monolith no longer mounts /api/rag; the gateway proxies that path to :4802.
 */
throw new Error(
  'rag.routes.js has moved to backend/services/rag-service — do not mount from the monolith'
)
