/**
 * Images routes — MOVED to backend/services/images-service/routes/images.routes.js.
 * The monolith no longer mounts /api/images; the gateway proxies that path to :4803.
 */
throw new Error(
  'images.routes.js has moved to backend/services/images-service — do not mount from the monolith'
)
